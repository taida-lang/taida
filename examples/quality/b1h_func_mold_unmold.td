// B-1h: 関数内モールド + unmold — Result[v, p]() ]=> x を関数内で実行
safeDiv x y =
  Div[x, y]() ]=> result
  result
=> :Int

stdout(safeDiv(10, 2).toString())
stdout(safeDiv(10, 0).toString())
// Expected: 5
// Expected: 0
